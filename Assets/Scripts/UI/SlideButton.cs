using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SlideButton : Button {

	// Use this for initialization
	void Start () {
		base.Start ();
	}
	
	// Update is called once per frame
	//void Update () {
	//	if (VirtualInput.horizontal * VirtualInput.horizontal + VirtualInput.vertical * VirtualInput.vertical > 0.98f) {
	//		interactable = true;
	//		VirtualInput.slide = true;
	//	} 
	//}
}
